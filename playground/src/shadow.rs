pub fn demo_shadow() {
    let x = 5;
    let x = x + 1;
    let x = x * 2;
    println!("Nilai dari x adalah {}", x);
}

pub fn trans_shadow() {
    let spaces = "   ";
    let spaces = spaces.len();
    println!("Panjang spasi: {}", spaces);
}